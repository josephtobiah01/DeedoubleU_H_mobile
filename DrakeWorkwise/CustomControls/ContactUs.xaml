<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="DrakeWorkwise.CustomControls.ContactUs"
             x:Name="contactUs">
    <Frame Margin="0,5">
        <Grid Padding="5" >
            <Grid.GestureRecognizers>
                <TapGestureRecognizer Command="{Binding LinkCommand, Source={x:Reference contactUs}}" CommandParameter="{Binding ContactNumber}"/>
            </Grid.GestureRecognizers>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="130"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Label HorizontalOptions="Start" Grid.Column="0"
                Text="{Binding Text}" FontAttributes="Bold" TextColor="Black"/>
            <Label HorizontalOptions="CenterAndExpand" Grid.Column="1"
                Text="{Binding ContactNumber}" TextColor="Gray"/>

        </Grid>
    </Frame>
</ContentView>
