<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:cnv="clr-namespace:DrakeWorkwise.Converters"
             x:Class="DrakeWorkwise.CustomControls.ValidateEntry">
    <ContentView.Resources>
        <cnv:ReverseBool x:Key="revBool"/>
        <cnv:BoolToColor x:Key="boolCol"/>
    </ContentView.Resources>
    <Grid Margin="0,0,0,10">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <Label Grid.Row="0" Text="{Binding Title}" TextColor="{Binding IsValid, Converter={StaticResource boolCol}, ConverterParameter='#000000|#A10909'}"
               FontSize="Medium" HorizontalOptions="FillAndExpand" HorizontalTextAlignment="Start" VerticalTextAlignment="Center" Margin="0,10,0,5"/>
        <Editor  Grid.Row="1" Text="{Binding Value}" Placeholder="{Binding Placeholder}" Keyboard="{Binding KeyBoard}" BackgroundColor="White" TextColor="Black" PlaceholderColor="LightGray"
               HorizontalOptions="FillAndExpand" Unfocused="Entry_Unfocused" Focused="Entry_Focused"  AutoSize="TextChanges"/>
        <!--<Label Grid.Row="2"  Text="{Binding Error}" IsVisible="{Binding IsValid, Converter={StaticResource revBool}}" FontSize="Small" TextColor="#A10909"/>-->
    </Grid>
</ContentView>
