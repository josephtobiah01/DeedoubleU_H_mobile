<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="DrakeWorkwise.CustomControls.TitleBar"
             x:Name="titleBar">
    <Grid Margin="3,5,8,5"
          ColumnSpacing="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <Label Grid.Column="0" 
               Text="{Binding TitleText, Source={x:Reference titleBar}}" 
               TextColor="White" 
               HorizontalOptions="StartAndExpand" 
               VerticalTextAlignment="Center" 
               HorizontalTextAlignment="Start" 
               FontSize="Medium"/>

        <Image Grid.Column="1" 
               Source="phone.png" 
               HorizontalOptions="EndAndExpand" 
               HeightRequest="30" 
               WidthRequest="30" 
               Aspect="AspectFit" 
               IsVisible="{Binding HasPhoneLink, Source={x:Reference titleBar}}">
            <Image.GestureRecognizers>
                <TapGestureRecognizer Command="{Binding CallUsCommand}" CommandParameter="CallUsPage"/>
            </Image.GestureRecognizers>
        </Image>
        
    </Grid>
</ContentView>
